# See structure here https://github.com/backstage/backstage/tree/master/packages/catalog-model/examples
apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: Golden Path Catalog
  description: A collection of all Backstage entities supporting Golden Path Demo
spec:
  targets:
    - ./all-templates.yaml
